/**
 * 
 */
package com.thirumal.java8.parallel;

import java.util.Iterator;
/**
 * @author Thirumal
 *
 */
public class Parallelization {
	Range range = new Range();
	public static void main(String[] str) {
		
	}
/*	
	private long countPrimes(int max) {
	    return range(1, max).parallel().filter(this::isPrime).count();
	}

	private Object range(int i, int max) {
		// TODO Auto-generated method stub
		return null;
	}

	private boolean isPrime(long n) {
	    return n > 1 && rangeClosed(2, (long) Math.sqrt(n)).noneMatch(divisor -> n % divisor == 0);
	}

	private Object rangeClosed(int i, long sqrt) {
		// TODO Auto-generated method stub
		return null;
	}
	*/
}
